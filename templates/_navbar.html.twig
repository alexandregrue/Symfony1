<nav class="navbar navbar-light bg-light justify-content-space_between">
    <a class="Wseries" href="#">
        <img src="{{ asset('build/images/logoWCS.png') }}" width="110" height="30" class="d-inline-block "
            alt="">
        Wild Séries
    </a>
    <a class="series" href="#">
        Nos séries
    </a>
    {% if app.user %}
        {{ app.user.email }}
            <a class="connection" href="{{ path('app_logout') }}"> Se déconnecter</a>
        {% else %}
            <a class="connection" href="{{ path('login') }}"> Se connecter</a>
    {% endif %}

</nav>